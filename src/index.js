import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';

export const maxRows = 24;
export const maxCols = 24;


ReactDOM.render(
  <App/>,
  document.getElementById("root")
);
